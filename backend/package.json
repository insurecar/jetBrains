{
  "name": "feedback-backend",
  "version": "1.0.0",
  "description": "Minimal Express backend for feedback endpoint",
  "main": "src/server.js",
  "scripts": {
    "build": "node --check src/server.js && node --check src/config/db.js && node --check src/controllers/feedbackController.js && rm -rf dist && mkdir -p dist && cp -R src/. dist",
    "start": "node src/server.js",
    "start:prod": "node dist/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^4.18.3",
    "mongodb": "^6.21.0"
  }
}
